const getter = {
  token: state => state.token,
  user: state => state.user,
  userTab: state => state.userTab,
  parkIdList: state => state.parkIdList,
  parkInfo: state => state.parkInfo,
  shoppingCart: state => state.shoppingCart,
};

export default getter;
